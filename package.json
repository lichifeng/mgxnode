{
  "name": "mgxnode",
  "version": "0.1.0",
  "description": "A node.js addon for parsing Age of Empires II record based on MgxParser.",
  "main": "mgxnode.js",
  "private": true,
  "dependencies": {
    "bindings": "~1.2.1",
    "node-addon-api": "^1.0.0"
  },
  "scripts": {
    "install": "cmake-js --prefer-clang -p 10 compile",
    "test": "node mgxnode.js"
  }
}
